services:
  app:
    build:
      context: .
      dockerfile: docker/app/Dockerfile
    environment:
      APP_PORT: "3000"
      TERMINAL_URL: "http://localhost:${HOST_TERMINAL_PORT:-7681}"
      TERMINAL_CONTROL_URL: "http://terminal:7682"
    ports:
      - "${HOST_APP_PORT:-3000}:3000"
    volumes:
      - .:/workspace
      - kiro-data:/root/.local/share/kiro-cli
      - kiro-config:/root/.kiro
      - kiro-cache:/root/.cache
    depends_on:
      - terminal
      - kiro-agent
      - lark-cli
      - runner
    command: ["bash", "-lc", "set -a; [ -f .env ] && source .env; set +a; node app/server.js"]

  terminal:
    build:
      context: .
      dockerfile: docker/kiro/Dockerfile
    environment:
      TERM: xterm-256color
      TMUX_SESSION: coldcase
      TERMINAL_CONTROL_PORT: "7682"
    ports:
      - "${HOST_TERMINAL_PORT:-7681}:7681"
    working_dir: /workspace
    command: ["bash", "-lc", "set -a; [ -f .env ] && source .env; set +a; exec bash /workspace/scripts/tmux-terminal-entrypoint.sh"]
    volumes:
      - .:/workspace
      - kiro-data:/root/.local/share/kiro-cli
      - kiro-config:/root/.kiro
      - kiro-cache:/root/.cache

  kiro-agent:
    build:
      context: .
      dockerfile: docker/kiro/Dockerfile
    working_dir: /workspace
    command: ["bash", "-lc", "set -a; [ -f .env ] && source .env; set +a; scripts/kiro-status.sh --watch"]
    volumes:
      - .:/workspace
      - kiro-data:/root/.local/share/kiro-cli
      - kiro-config:/root/.kiro
      - kiro-cache:/root/.cache

  lark-cli:
    build:
      context: .
      dockerfile: docker/lark/Dockerfile
    working_dir: /workspace
    command: ["bash", "-lc", "set -a; [ -f .env ] && source .env; set +a; npx -y @getlark/cli --version && tail -f /dev/null"]
    volumes:
      - .:/workspace

  runner:
    build:
      context: .
      dockerfile: docker/runner/Dockerfile
    working_dir: /workspace
    command: ["bash", "-lc", "java -version && tail -f /dev/null"]
    volumes:
      - .:/workspace

volumes:
  kiro-data:
  kiro-config:
  kiro-cache:
